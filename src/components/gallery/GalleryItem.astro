---
import { Image } from "astro:assets";
import type { CollectionEntry } from "astro:content";
import { resolveGalleryImage } from "../../lib/photos";

interface Props {
  photo: CollectionEntry<"gallery">;
}

const { photo } = Astro.props;
const image = resolveGalleryImage(photo.data.image);
const tagsAttr = photo.data.tags.join(" ");
---

<button
  type="button"
  class="gallery-item group block w-full overflow-hidden rounded-md bg-muted focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
  data-slug={photo.id}
  data-tags={tagsAttr}
  aria-label={photo.data.title}
>
  <Image
    src={image}
    alt={photo.data.title}
    widths={[400, 800, 1200]}
    sizes="(min-width: 1280px) 25vw, (min-width: 640px) 33vw, 50vw"
    loading="lazy"
    decoding="async"
    class="block h-auto w-full transition-transform duration-500 ease-out group-hover:scale-[1.02]"
  />
</button>
